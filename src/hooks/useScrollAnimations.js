// This file is intentionally empty.
// The old useScrollAnimations hook has been replaced by the scene-based
// animation system where each component handles its own GSAP animations.
// This file is kept as a no-op export to avoid breaking any remaining imports.

export function useScrollAnimations() {
  // no-op — animations are now handled per-scene component
}
